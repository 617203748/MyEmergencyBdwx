<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taxi.dao.InstitutionDao">

    <select id="selectModel" parameterType="HashMap"
            resultType="T_institution">
		SELECT * FROM t_institution WHERE 1 = 1
	</select>

</mapper> 