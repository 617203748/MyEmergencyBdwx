1、下载
wget http://subversion.tigris.org/downloads/subversion-deps-1.6.12.tar.bz2
wget http://subversion.tigris.org/downloads/subversion-1.6.12.tar.bz2

2、解压
tar jxvf subversion-deps-1.6.12.tar.bz2
tar jxvf subversion-1.6.12.tar.bz2

3、准备安装
mkdir /mydata/setup/subversion
cd subversion-1.6.12

4、安装
./configure --prefix=/mydata/setup/subversion
make && make install
 
5、配置环境
vim /etc/profile
	#Svn
	export SUBVERSION=/mydata/setup/subversion
	export PATH=$PATH:$SUBVERSION/bin
 
6、验证是否安装成功
svn help

																		客户端使用
1、目标检出
	移动到nginx或者apache的工作目录下运行
 	svn checkout svn://svn.sxbdjw.cn/company_codes/eme/v1.0.1/
 	输入root账号的密码
 	输入svn账号
 	输入svn密码
 	
 	检出后更新则 svn up
 
 
																		服务端使用
1、创建任务
	repository 文件夹是用来存放项目的
	test 文件夹是测试项目（随便起）
	
	mkdir /mydata/setup/subversion/repository
	cd /mydata/setup/subversion/
	chmod -R 777 repository
	svnadmin create /mydata/setup/subversion/repository/sunny
	
	1）vim /mydata/setup/subversion/repository/sunny/conf/svnserve.conf    //配置版本库信息和用户文件和用户密码文件的路径、版本库路径
			anon-access = none		# 使非授权用户无法访问
			auth-access = write		# 使授权用户有写权限 
			password-db = passwd	# 指明密码文件路径 
			realm = sunny			# 认证命名空间，subversion会在认证提示里显示，并且作为凭证缓存的关键字。 
			
	2）vim /mydata/setup/subversion/repository/sunny/conf/authz			//文件,创建svn组和组用户的权限
		[group]
		sunny = zzz,dd   	//创建一个sunny的组，并指定两个用户gep和wce
		[/]             	//制定根目录下的权限
		@sunny = rw     		//sunny组用户权限为读写
		* = r           	//其他用户只有读权限
			
	3）vim /mydata/setup/subversion/repository/sunny/conf/passwd			//创建或修改用户密码	
		zzz = zzz_admin
		zgp = zgp_admin	
		dd = dd_admin
		
2、启动SVN
	1）多库启动方式
		svnserve -d -r /mydata/setup/subversion/repository/ 	
	2）单库启动方式
		svnserve -d -r /mydata/setup/subversion/repository/test
		
	ps -e|grep svnserve
		









		
			
			
			