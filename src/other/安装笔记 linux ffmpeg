												安装libmp3lame
# cd /mydata/setup/
# tar zxvf lame-398.tar.gz
# cd lame-398.tar.gz
# ./configure   
# make && make install 

												安装FFmpeg
# tar xf ffmpeg-2.8.4.tar.bz2
# cd ffmpeg-2.8.4/
# ./configure --enable-libmp3lame --disable-yasm
# make
# make install

# vim /etc/profile
	末尾添加
	export FFMPEG_HOME=/usr/local/ffmpeg 
    export PATH=$FFMPEG_HOME/bin:$PATH
	    如果原来已经配置了java环境变量的话
	    export PATH=$FFMPEG_HOME/bin:$JAVA_HOME/bin:$PATH  
# source etc/profile

# vim /etc/ld.so.conf
# 末尾添加
	/mydata/setup/ffmpeg/lib
# ldconfig

# ffmpeg -version




如果报错： ffmpeg: error while loading shared libraries: libmp3lame.so.0: cannot open shared object file: No such file or directory
# ln -s /usr/local/lib/libmp3lame.so.0.0.0 /usr/lib64/libmp3lame.so.0



